function isNumber(x) {
    if (!isNaN(a) &&!isNaN(b)) {
        return [sum(a, b), diff(a, b), product(a, b), qout(a, b)]
    } else {
        alert("bitte geben sie eine gültige Zahl ein!")
    }
}

checkIfNumber(a, b)



function sum(a, b) {
    return a + b 
}
function diff(a, b) {
    return a - b 
}
function product(a, b) {
    return a * b 
}
function quot(a, b) {
    return a / b 
}